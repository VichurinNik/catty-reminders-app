services:
  catty-app:
    build: .
    container_name: catty-app
    restart: always
    environment:
      - DEPLOY_REF=${DEPLOY_REF}
    ports:
      - "${HOST_APP_PORT}:8181"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./reminder_db.json:/app/reminder_db.json
